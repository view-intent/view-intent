export namespace ViewIntent {
	// export module Controller {
	// 	export var CurrentViewId: string = null;
	// 	export var Views: { [viewId: string]: AbstractViewAdapter } = {};
	// 	export var Holders: { [holderId: string]: Holder } = {};
	// 	// export var ShowingViewsIds: Array<string> = [];


	// 	export interface DynamicViewInfo extends DynamicNav.DynamicHeadInfo {
	// 		url?: string | null;
	// 		template?: string | null;
	// 		model?: string | JSON | null;
	// 		docRendered?: boolean | null | undefined;
	// 		actionMethod?: string | null;
	// 		//inDoc?: boolean; // are the baseElement already in the page document
	// 	}
	// }
}

